# Internal to-do — never rendered

The build reads `content/stories`, `content/collections`, `content/partners` and
`content/sources`. It never reads this directory. Editorial notes belong here,
or in the `internal:` field of a record — a field the renderer does not read.

## Before deployment
1. Create and monitor hello@isleconnect.co.uk (SPF, DKIM, DMARC; test to Gmail and Outlook).
2. Read and approve privacy.html, accessibility.html, terms.html.
3. Approve the About copy.
4. Decide on The Dell Cafe — the Nearby block stays omitted until inclusionApproved is true.
5. Delete the stale pages listed in DEPLOY.md from the repository.

## Content gaps
- Five of nine trail stops unnamed — see TRAIL-STOPS.md.
- puckpool-now.jpg — closes the Then/Now exception on the flagship wartime story.
- Vector logo — header mark is still a 136px PNG.
- WebVTT caption tracks for both films.
- Contributor bios and portraits (the Behind the story section is removed, not published empty).
